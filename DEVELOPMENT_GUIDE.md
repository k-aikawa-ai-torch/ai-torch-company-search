# AI torch 社内業務システム 開発ガイド

このドキュメントは、AI torchの社内業務システム（企業検索・候補者管理・パイプライン・ダッシュボード・詳細KPI・予実管理）を、Claudeと一緒に保守・改善していくためのガイドです。

対象読者：エンジニアではないが、システムの利用経験があり、多少の用語は分かるメンバー。

---

## 0. 新しいチャットを始めるときにコピペする文言

Claudeとの会話は、ブラウザ（claude.ai）でもClaude Desktop / Claude Codeでも構いません。新しいチャットを開いたら、まず以下をそのまま貼り付けてください（〇〇の部分だけ書き換えてください）。

```
AI torch社内システム（人材紹介エージェント向け、Supabase+GitHub Pagesの
企業検索・候補者管理・パイプライン・ダッシュボード・詳細KPI・予実管理）
の改修をお願いしたいです。

まず下記の開発ガイドを読んでから始めてください。
https://raw.githubusercontent.com/k-aikawa-ai-torch/ai-torch-company-search/main/DEVELOPMENT_GUIDE.md

今回やりたいことは：
〇〇
```

Claude自身の「記憶」機能は、それを使っている人のアカウントに紐づいています。健太さんの会話でClaudeが覚えていることを、他のメンバーのアカウントのClaudeは自動的には知りません。**このガイドが、誰の会話であっても参照できる「共通の記憶」の役割を果たします**。ここに書かれていない最近の変更点や経緯があれば、その都度この文言の後ろに補足してください。

---

## 1. システム概要

AI torch（人材紹介エージェント）の社内業務を1つに集約した自社システムです。もともとNotion・Googleスプレッドシート・GASに分散していた情報を統合しました。

### 画面構成（7ページ、2つのGitHubリポジトリ）

**リポジトリ①：`ai-torch-company-search`**
| ページ | ファイル | 内容 |
|---|---|---|
| ホーム | index.html | 全体のトップページ |
| 企業検索 | companies.html | 企業一覧・求人条件検索（カード展開式） |
| 候補者管理 | candidates.html | 候補者の登録・編集・メモ・提案求人・ファイル添付 |
| パイプライン | pipeline.html | 候補者ごとの提案求人の進捗（フェーズ管理） |

**リポジトリ②：`ai-torch-dashboard`**
| ページ | ファイル | 内容 |
|---|---|---|
| ダッシュボード | dashboard.html | メンバー別の月次サマリー |
| 詳細KPI | kpi.html | 媒体別・職種別などの詳細分析 |
| 予実管理 | financials.html | 売上・粗利の予実、事務処理チェックリスト |

どちらのリポジトリも共通の見た目・共通のログイン・共通の横断検索（Ctrl+K）を持っています。共通CSSと共通JS（検索機能）は`ai-torch-company-search`側の`assets/`フォルダにあり、`ai-torch-dashboard`側の3ページはそこを絶対URLで参照しています。

### インフラ構成
- **画面（フロントエンド）**：素のHTML/CSS/JavaScript。ビルドツールや框組み（React等）は使っていません。GitHub Pagesでそのまま公開されています
- **データベース**：Supabase（PostgreSQL）。プロジェクトID `vqunibhzmvlckqbydgea`（Seoulリージョン）
- **認証**：Supabase Auth（メンバーのメールアドレス・パスワードでログイン）
- **企業データの同期**：Google Apps Script（GAS）が定期実行され、スプレッドシートの企業情報をGitHub（companies.json）とSupabaseの両方に反映しています

---

## 2. 開発の進め方

1. 上記「0. 新しいチャットを始めるときにコピペする文言」でチャットを開始する
2. やりたいことを箇条書きでざっくり伝えてOK。細かいことを詰めきる必要はありません。Claudeが不明な点を質問してくるので、それに答えながら進めます
3. Claudeが修正したファイル（.html等）を提示してくれます
4. 下記「4. GitHubへの反映方法」の手順でアップロードする
5. 実際の画面を開いて動作確認する（Claudeが確認すべきポイントを案内してくれます）
6. 問題があれば、症状をそのまま伝えて修正してもらう

### 会話が長くなって重くなったら
同じチャットで何時間もやり取りを続けると、ブラウザが重くなったりクラッシュしやすくなります。動作がおかしくなったら、新しいチャットを開いて「0.」の文言から再開してください。今回のガイドと、Supabase上のデータを見れば、Claudeは経緯を推測しながら再開できます（できれば直前にやっていたことも一言添えてください）。

---

## 3. データベース（Supabase）の主なテーブル

| テーブル名 | 内容 |
|---|---|
| members | 社員（相川・永井・會澤・中村の4名） |
| companies | 取引企業（約200社） |
| candidates | 候補者（約660名） |
| pipeline | 候補者ごとの提案求人（約1,900件） |
| pipeline_phase_history | パイプラインのフェーズが変わった履歴（売上計上の根拠） |
| fee_rates | 手数料率の履歴 |
| other_revenue | 人材紹介以外の売上（コンサル・RPO等） |
| targets | メンバー別の月次目標 |
| templates | 候補者メモのテンプレート |
| manual_kpi_inputs / member_monthly_manual | 過去データ移行用の手入力KPI数値 |
| custom_metrics | 詳細KPIのカスタム指標 |

ファイル添付は`candidates-files`という名前のStorageバケットに保存されています。

### RLS（行レベルセキュリティ）について
すべてのテーブルで、ログイン済みメンバーだけが読み書きできる`member_only`という統一ポリシーを使っています。**新しいテーブルを作るときは、必ず「GRANT（権限付与）」「ポリシー設定」「RLSを有効化（ENABLE ROW LEVEL SECURITY）」の3点セットを行ってください**。どれか1つでも忘れると、正常に動かない、またはセキュリティが甘いままになります。

---

## 4. GitHubへの反映方法

Claudeが作った・直したファイルは、GitHubの画面からアップロードして反映します。

1. 対象のリポジトリ（`ai-torch-company-search` または `ai-torch-dashboard`）をブラウザで開く
2. 画面右上あたりの「Add file」→「Upload files」をクリック
3. Claudeが用意したファイルを、この画面にドラッグ＆ドロップする（同じ名前のファイルは自動的に上書きされます。複数ファイルまとめてドロップしてもOKです）
4. 画面下の「Commit changes」ボタンを押す
5. 数十秒〜数分待つと、実際のページ（`https://k-aikawa-ai-torch.github.io/ai-torch-company-search/候補者管理.html` など）に反映されます

---

## 5. 【重要】これだけは崩さないでほしい設計ルール

過去の不具合対応の中で分かった、崩すと事故につながるルールです。Claudeにはこのガイドを読ませたうえで作業させてください。

- **決定ベースの売上計上の根拠は `pipeline_phase_history`**（「内定承諾」に初めて到達した日時）であり、`pipeline`テーブルの日付列そのものではない
- **発生ベースの売上計上は `pipeline.hire_date`（入社日）基準**
- **返金は `pipeline.refund_date` の月にマイナス計上**
- **粗利の計算は常に `candidates.revenue_forecast`（売上見込み）基準**。`revenue_yomi`（売上ヨミ金額）は別物なので混同しない
- **`financials.html`でデータを保存する処理は、必ず `renderKeepScroll()` を使う**（`render()`だと保存のたびに画面が先頭に戻ってしまう）
- **GASからSupabaseへの企業データ同期は必ずupsert（更新 or 挿入）で行う**。「一度全部消してから入れ直す」方式は、他のテーブルからの参照が一瞬途切れて事故るので絶対に禁止
- **1000件を超えるテーブルをページング取得（`.range()`）するときは、必ず`.order('id')`など一意な並び順を指定する**。指定しないと境界がズレて一部のデータが取得漏れになる不具合が過去に発生した
- **`candidates.theoretical_salary`（理論年収）は信頼性が低い**。旧システムで手数料率40%固定を前提に逆算された値のため、実際の手数料率とズレている候補者が多い。年収分析で使う際は個別確認が必要
- **中村さん（assistant@ai-torch.com）は売上を作る立場ではない**ため、KPI集計・ランキング・メンバー比較の対象からは除外する（`SALES_MEMBERS`という仕組みで制御している）

---

## 6. 【最重要】危険な操作をするときは、必ず会話上で確認を取ってから実行する

これは今回のガイド整備のきっかけになった、最も重要なルールです。

**Claudeは、以下のような「元に戻せない」「データが失われる」操作を行う場合、実行する前に必ず会話の中で内容を提示し、明確な承諾（「はい」「実行してください」等）を得てから実行してください。確認なしに実行することは禁止です。**

対象となる操作の例：
- テーブルやカラムの削除（`DROP TABLE` / `DROP COLUMN` / `ALTER TABLE ... DROP`）
- 行データの削除（`DELETE`、`TRUNCATE`）
- 既存データを大量に書き換える`UPDATE`（対象件数が多い、または元の値を復元できない場合）
- RLSポリシーや権限設定を緩める変更（セキュリティが下がる方向の変更）
- 候補者・企業・パイプラインなど、業務データを恒久的に消す操作（候補者管理の「完全に削除」ボタンなど、システム内の機能を使う場合も同様）

確認の際は、「何を」「どのテーブル・どの範囲に対して」「なぜ」「元に戻せるかどうか」を一言でよいので明示してから実行の可否を聞くようにしてください。これは会話中の毎回の依頼で再度伝える必要はなく、このガイド自体に明記されたルールとして常に有効です。

---

## 7. 困ったときは

- **画面が思った通りに動かない**：症状（どのページで・何をしたら・どうなったか）をできるだけ具体的にClaudeに伝えてください。スクリーンショットの説明でも構いません
- **前のやり取りの経緯が分からなくなった**：このガイドと、Supabase上の実データ、GitHub上の実際のファイルを見れば、Claudeはある程度経緯を推測できます。分かる範囲で「直前まで何をしていたか」を一言添えてもらえると助かります
- **このガイド自体を更新したい**：システムの仕様や設計ルールが変わったときは、このガイド（`DEVELOPMENT_GUIDE.md`）自体もあわせて更新するようClaudeに依頼してください。ガイドが古いままだと、次に会話する人が古い前提で進めてしまいます
